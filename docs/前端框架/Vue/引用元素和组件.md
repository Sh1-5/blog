---
title: 引用元素和组件
---

## 获取元素和组件

在 Vue 中，不推荐直接进行 DOM 操作

给元素或组件添加一个`ref`属性

在组件实例中，含有`$refs`对象，包含这个组件实例中所有`ref`对应的元素或组件

如果是组件，可以获取到组件的`data`和`methods`

## 获取父组件和根组件

通过`$parent`和`$root`

`$parent`和`$root`是 proxy

注意 ⚠️：Vue3 移除了`$children`

## 获取根元素

通过`$el`
