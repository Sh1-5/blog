---
title: 需要注意的点
---

## data 属性

vue2 中根组件的 data 属性可以是一个对象或函数，vue3 中根组件的 data 属性必须是一个函数

## this 指向问题

[this 指向问题](https://mp.weixin.qq.com/s/hYm0JgBI25grNG_2sCRlTA)

## virtual node

virtual node，虚拟节点，本质上是一个 javascript 对象

template -> virtual node

虚拟 dom：virtual node tree

## watch

函数形式：

```javascript

data() {
  return {
    message: 'Hello Vue3'
  }
},
watch: {
  message(newValue, oldValue) {
    console.log(newValue, oldValue)
  }
}
```

对象形式：

```javascript
data() {
  return {
    info: {
      name: 'Sh1-5',
      age: 18
    }
  }
},
watch: {
  info: {
    deep: true, // 深度侦听
    immediate: true, // 立即执行,
    handler(newValue, oldValue) {
      console.log(newValue, oldValue)
    }
  }
}
```

⚠️：在变更 (不是替换) 对象或数组时，旧值将与新值相同，因为它们的引用指向同一个对象/数组。Vue 不会保留变更之前值的副本。

## v-model

```
<input type="text" v-model="message" />
<input type="text" :value="message" @input="message = $event.target.value" />
```
