---
title: 动态组件
categories:
  - 前端
tags:
  - Vue
---

## 什么是动态组件

可以动态显示不同组件的组件

`<component :is="..."></component>`是一个内置组件

## 基本使用

```html
<component :is="MyComponent"></component>
```

`:is`可以是被注册的组件名或者导入的组件对象
