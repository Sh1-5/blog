# 项目优化建议（待完成）

以下是尚未实施的优化项。已完成的 23 项优化已从本文档中移除。

---

## 待完成项

### 1. 添加 API 速率限制

**文件**: `src/app/api/visit-stats/route.ts`

**当前状态**: 已添加服务端 5 分钟内存缓存，但尚未添加速率限制

**问题**:
- 没有速率限制，可能被恶意请求滥用

**修复方案**: 可使用中间件或第三方库（如 `upstash/ratelimit`）实现

---

### 2. 配置包分析器

**需要安装依赖**: `@next/bundle-analyzer`

```bash
npm install -D @next/bundle-analyzer
```

```javascript
// next.config.mjs
import bundleAnalyzer from '@next/bundle-analyzer'

const withBundleAnalyzer = bundleAnalyzer({
  enabled: process.env.ANALYZE === 'true',
})

export default withBundleAnalyzer(nextConfig)
```

---

### 3. 添加预提交钩子

**需要安装依赖**: `husky` + `lint-staged`

```bash
npm install -D husky lint-staged
npx husky install
```

```json
// package.json
{
  "lint-staged": {
    "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
    "*.{json,md}": ["prettier --write"]
  }
}
```

---

## ✅ 已完成的优化（共 23 项）

1. 启用图片优化 — `next.config.mjs`
2. 博客文章静态生成 — `blogs/[slug]/page.tsx`
3. 博客内容缓存 — `blogs.ts`
4. 分析服务懒加载 — `analytics.tsx`
5. 修复类型安全（移除 `@ts-expect-error`）— `blogs.ts`
6. 懒加载重型组件（IconCloud、TweetGrid、MarqueeVertical）— `page.tsx`
7. 添加错误边界 — `error.tsx`、`blogs/error.tsx`
8. 合并 RSS Feed 重复逻辑 — `feed/route.ts`
9. Visit Stats API 服务端缓存 — `visit-stats/route.ts`
10. 博客丰富元数据（OG、Twitter Card、JSON-LD）— `blogs/[slug]/page.tsx`
11. 删除生产环境 console.log — `sitemap.ts`
12. 修复关于页面元数据 — `about/page.tsx`
13. 提取重复 ClientTweetCard 组件 — `ClientTweetCard.tsx`
14. Icon Cloud 数据缓存 — `icon-cloud.tsx`
15. GitHubSnake 改用 Next.js Image — `GitHubSnake.tsx`
16. 添加博客加载状态 — `blogs/loading.tsx`
17. 删除未使用的导入（cheerio 等）— `feed/route.ts`
18. Next.js 安全/性能配置（poweredByHeader、compress、安全头、静态资源缓存）— `next.config.mjs`
19. 首页 ISR 缓存（revalidate: 60）— `page.tsx`
20. MarqueeVertical 骨架屏占位符（修复 CLS）— `page.tsx`
21. Header 滚动事件 rAF 节流 — `Header.tsx`
22. TypingAnimation 性能优化（useRef 替代重复 state/effect）— `typing-animation.tsx`
23. 统一图标库为 phosphor-icons，移除 lucide-react — 多文件
24. 移除 react-syntax-highlighter（~130KB），统一使用 rehype-prism 编译时高亮 — `MdxComponents.tsx`
25. 清理未使用依赖（cheerio、lucide-react、react-syntax-highlighter）— `package.json`
